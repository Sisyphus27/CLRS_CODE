//
// Created by zy on 2021/9/8.
//

#include "_Bucket_sort.h"

void _Bucket_sort::_bucket_sort(vector<int> &A) {
    int n = A.size();
    vector<int> B(n, 0);
    for (int i = 0; i < n; ++i) {

    }
}
